	Ближайшее:

см. TODO в коде

Отключать джойстик при невыборе его конкретного режима (Joystick / Keymapper)
только если присутствует клавиатура. Задействовать environment-вызов
RETRO_ENVIRONMENT_GET_INPUT_DEVICE_CAPABILITIES

Реализовать настройку keymapper'а джойстика через интерфейс RetroArch

+Реализовать полную поддержку загрузки из CAS-формата

+Автозагрузка из CAS (как например автозапуск контента ROM при запуске MSX)

Конвертер форматов: поддержка форматов WAV, CAS, BIN (i.e. block? or chunk?)

Компиляция под Linux и для ARM: Ordoid-XU4 + Lakka


	Рефакторинг:

Спрятать все где возможно в анонимные пространства имен

Хандлеры указывают на невалидные (освобожденные) объекты, если имела место
их релокация. Придумать общий принцип-предохранитель
                                      
Оценить необходимость тотального перевода C-стиля организации
глобального кода на набор синглтонов

Заменить понятие record в audio.cpp на block (предварительно) там, 
где это означает одну запись, идущую после начального тона на кассете

playMedia -> playbackMedia, recordMedia -> recordingMedia

non-copyable классы реализовать при помощи non-copyable членов?


	Попозже:

Включение с клавиатуры режимов speedup/fwdScan во время вывода на ленту
(разобрать в bios процедуры вывода на ленту)

Разобраться с косяком модуляции сигнала при выводе на ленту (сделать вывод
таймингов фронтов/спадов в отладочный файл)

Сделать ввод/вывод в edge-формат во время эмуляции
Сделать конвертер из wav в edge-формат и наоборот
